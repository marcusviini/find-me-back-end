syntax = "proto3";


message SignUpRequest {
    string nome = 1;
    string email = 2;
    string password = 3;
}

message SignUpResponse {
    string message = 1;
    string error = 3;
}

service userService {
    rpc  signUp (SignUpRequest) returns (SignUpResponse);
}
